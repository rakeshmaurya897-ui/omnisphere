{
  "buildCommand": "cd src/frontend && pnpm install && pnpm run build",
  "outputDirectory": "src/frontend/dist",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}